<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialog_acr_custom"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_dark">

    <!-- Horizontal layout: Title | Ratings | Button -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Title on the left -->
        <TextView
            android:id="@+id/rate_caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/rate_ACR_caption_short"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginEnd="32dp" />

        <!-- Rating options - vertical list in center -->
        <LinearLayout
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/rating_container_background">

            <!-- Excellent -->
            <RadioButton
                android:id="@+id/radioButtonExcellent"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:button="@null"
                android:background="@drawable/rating_item_selector"
                android:gravity="center"
                android:text="@string/acr_excellent"
                android:textColor="@color/rating_text_color"
                android:textSize="16sp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#505050" />

            <!-- Good -->
            <RadioButton
                android:id="@+id/radioButtonGood"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:button="@null"
                android:background="@drawable/rating_item_selector"
                android:gravity="center"
                android:text="@string/acr_good"
                android:textColor="@color/rating_text_color"
                android:textSize="16sp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#505050" />

            <!-- Fair -->
            <RadioButton
                android:id="@+id/radioButtonFair"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:button="@null"
                android:background="@drawable/rating_item_selector"
                android:gravity="center"
                android:text="@string/acr_fair"
                android:textColor="@color/rating_text_color"
                android:textSize="16sp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#505050" />

            <!-- Poor -->
            <RadioButton
                android:id="@+id/radioButtonPoor"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:button="@null"
                android:background="@drawable/rating_item_selector"
                android:gravity="center"
                android:text="@string/acr_poor"
                android:textColor="@color/rating_text_color"
                android:textSize="16sp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#505050" />

            <!-- Bad -->
            <RadioButton
                android:id="@+id/radioButtonBad"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:button="@null"
                android:background="@drawable/rating_item_selector"
                android:gravity="center"
                android:text="@string/acr_bad"
                android:textColor="@color/rating_text_color"
                android:textSize="16sp" />

        </LinearLayout>

        <!-- Submit button on the right -->
        <Button
            android:id="@+id/buttonSendRating"
            android:layout_width="120dp"
            android:layout_height="48dp"
            android:layout_marginStart="32dp"
            android:text="@string/rate"
            android:enabled="false"
            android:visibility="invisible"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/submit_button_background"
            android:textColor="@color/white" />

    </LinearLayout>

</FrameLayout>
